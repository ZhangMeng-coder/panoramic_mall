创建一个新的微服务：用户管理服务（admin）
需要创建对应的表：
用户表、角色表、用户角色关系表、权限表（权限要分目录，页面，按钮，层级递减，并且需要创建对应的权限字符串，如system:user:list或system:user:add）、角色权限关系表

需要实现的功能：
用户管理（CRUD, 用户分配角色）
角色管理（CRUD, 角色分配用户（注：分配页面中展示非该角色的用户）, 角色分配权限（注：树形结构选择页面，上级可以控制下级全选或全取消））
权限管理（CRUD,树形结构展示和编辑，前端页面所需要的目录接口也从这个功能上提供）

以上就是需要实现的全部功能，先做数据的基础管理，其他功能之后再说