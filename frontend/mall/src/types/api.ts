/* ============================================================================
   跨域通用的响应形状 —— 只在**多个 api 模块**共用时才放这里；
   单个模块的入出参放各自的 types/<域>.ts（如 types/catalog.ts）。
   字段定义以后端 VO / DTO 类为准，这里只是前端侧的镜像。
   ========================================================================== */

/** 后端分页结果形状（与 common 的 PageResult 同源） */
export interface PageResult<T> {
  total: number
  records: T[]
}
