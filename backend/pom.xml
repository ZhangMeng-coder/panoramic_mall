<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>4.0.7</version>
        <relativePath/>
    </parent>

    <groupId>com.panoramic</groupId>
    <artifactId>panoramic-mall-backend</artifactId>
    <version>1.0.0-SNAPSHOT</version>
    <packaging>pom</packaging>
    <name>panoramic-mall-backend</name>
    <description>全景商城后端父项目</description>

    <modules>
        <module>gateway</module>
        <module>goods-center</module>
        <module>admin</module>
        <module>common</module>
    </modules>

    <properties>
        <java.version>21</java.version>
        <spring-cloud.version>2025.1.2</spring-cloud.version>
        <spring-cloud-alibaba.version>2025.1.0.0</spring-cloud-alibaba.version>
        <!-- MyBatis-Plus 版本（Spring Boot 4 需用专用 starter，3.5.15 起正式兼容） -->
        <mybatis-plus.version>3.5.16</mybatis-plus.version>
        <!-- Nacos 注册/配置中心统一连接：子模块 application.yml 中以 @nacos.xxx@ 引用，
             由 spring-boot-starter-parent 自带的 Maven 资源过滤在构建时替换 -->
        <nacos.server-addr>127.0.0.1:8848</nacos.server-addr>
        <nacos.username>nacos</nacos.username>
        <nacos.password>nacos</nacos.password>
        <!-- JWT 库版本（admin/goods-center 经 common、gateway 单独引用，统一管理） -->
        <jjwt.version>0.12.6</jjwt.version>
        <!-- 鉴权统一值（jwt-secret/jwt-expire-seconds/redis-prefix/header-name）已收敛到
             Nacos 共享配置 nacos-config/auth.yml，签发(admin)/校验(gateway)/解析(goods-center+common)
             三端同源，此处不再重复定义（见 nacos-config/ 目录） -->
    </properties>

    <dependencyManagement>
        <dependencies>
            <!-- Spring Cloud -->
            <dependency>
                <groupId>org.springframework.cloud</groupId>
                <artifactId>spring-cloud-dependencies</artifactId>
                <version>${spring-cloud.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
            <!-- Spring Cloud Alibaba -->
            <dependency>
                <groupId>com.alibaba.cloud</groupId>
                <artifactId>spring-cloud-alibaba-dependencies</artifactId>
                <version>${spring-cloud-alibaba.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
            <!-- MyBatis-Plus（common 与 goods-center 共用，集中管理版本） -->
            <dependency>
                <groupId>com.baomidou</groupId>
                <artifactId>mybatis-plus-spring-boot4-starter</artifactId>
                <version>${mybatis-plus.version}</version>
            </dependency>
            <!-- 分页插件依赖（3.5.9+ 拆分，需与 starter 同版本） -->
            <dependency>
                <groupId>com.baomidou</groupId>
                <artifactId>mybatis-plus-jsqlparser</artifactId>
                <version>${mybatis-plus.version}</version>
            </dependency>
            <!-- JWT（jjwt 0.12.x：api/impl/jackson 三件套） -->
            <dependency>
                <groupId>io.jsonwebtoken</groupId>
                <artifactId>jjwt-api</artifactId>
                <version>${jjwt.version}</version>
            </dependency>
            <dependency>
                <groupId>io.jsonwebtoken</groupId>
                <artifactId>jjwt-impl</artifactId>
                <version>${jjwt.version}</version>
            </dependency>
            <dependency>
                <groupId>io.jsonwebtoken</groupId>
                <artifactId>jjwt-jackson</artifactId>
                <version>${jjwt.version}</version>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <dependencies>

    </dependencies>
</project>
