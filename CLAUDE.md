# 项目规约（Panoramic Mall）

> 本文件是**唯一的开发规约**，每次会话自动加载，故刻意写薄 —— 只写**流程、口径、元规则**。
> **不重述**已有落点的事实：接口契约 → [`docs/contracts/`](docs/contracts/README.md)；业务规则（数据权限 / 审核门禁 / 锁定 / 推导量）→ 各模块 README 的「三、职责与边界」；分层与依赖方向 → 根 [`README.md`](README.md) + [`backend/README.md`](backend/README.md) + 各模块 README 的「一、架构位置」；前端视觉与结构 → [`frontend/mall/README.md`](frontend/mall/README.md)。
> **同一事实不写两遍** —— 重述就是造出第二个会漂移的地方。

## 一、工作方式

**需求 → 契约 → 实现**，三步，前一步过门才进下一步。

1. **需求拓展**。输入是你提交的业务需求，或你写的需求文档 —— 载体是仓库根 **`todo.md`**（用文本文件是因为 CLI 不便写入大段内容）。我据此**拓展为详细需求**，**人工确认**后才进下一步。
   ⚠ 详细需求**任务完成后即废弃，不留底、不长期保存**。
2. **契约先行**。按序判断：① 现有契约**满足** → 直接用，不动契约；② 不满足但**改一下**能满足 → **改**现有契约；③ 都不满足 → **才新增**契约。
   **新增**的标 `待实现`；**修改的不设标记**（理由见下）。契约**先于实现落盘**。
3. **实现任务**。
   - **粒度**：一个任务**只针对一个服务**。**唯一例外** —— 现有项目的**修改**类改动若 BFF 需同步改，则**一起改（同一任务）**；若无影响，或**业务域新增接口**，只处理该服务。
   - **范围**：任务内只看该服务的契约 + 项目结构，不做其他处理。
   - **收尾**：摘掉自己那行的 `待实现` 并回填行号 —— 属**任务内**动作（不摘检查器报错），不是「其他处理」。

**为什么修改类不设 `待修改`**：契约行的键是 `方法 路径`。改路径会让检查器**同时**报「幽灵行」+「代码有、契约表没有」；只改类型则标 `待实现` 会撞**反向哨兵**（路径没变，代码里"有"该接口）。要让检查器自动分辨「新增 / 修改」，得把它从**存在性核对**升级成**形状比对**，而它只比对权限串、没有签名数据可依；退而用「路径在代码里存在 = 修改」推断，会让**忘摘标记永远检不出**。故修改类改动走「契约表与代码同一批改完」。

## 二、审计与精简流程

- **拒绝逐任务审计**。审计只在这两处发生：① **人工审核生成代码**（你自己看）；② **任务影响 ≥2 个服务时开启整体审计** —— **派子代理跑**，且审**整支**（只审一半会漏掉两侧不一致：接口 / 防线 / 降级）。
- **精简流程**：台账只用四列（任务 / commit / 裁决 / 残留）；子代理报告 ≤30 行固定模板；修复轮不落 brief；**计划 / spec / 台账一律不入库**（`docs/superpowers/**`、`.superpowers/**`、`.scratch/**` 均已 gitignore）；评审包用完即删。

## 三、验证口径

- **止步于编译通过**。前端没有编译，门是 **`vue-tsc --noEmit && vite build`**（类型不过即构建失败）。
- **不允许**：起服务、前端 dev/preview、`mvn test`、接口测试、`curl` 打接口。**你当场明确要求**除外（例外下只起目标服务，见第五节）。
- **纯配置 / 文档改动跳过编译**：改动文件**全部**是 `*.yml` / `*.yaml` / `*.properties` / `*.json` / `*.md` 才跳；**只要含一个代码文件**（`.java` / `.ts` / `.vue`）照常验。替代自检走**文本级**核对（引用的文件 / 键确实存在、该删的写法无残留）。
- **命令写法**：nodejs 与 maven 均已在 PATH —— 一律写 `npm` / `mvn`，**不写绝对路径**。

## 四、数据库改动与备份

**触发**：在**现有表**上做表结构修改、或大量测试数据灌入时 → 备份。**纯新增（建新表 / 加列）不备份**（可重放）。判据是「会不会销掉已有数据」。

**方式** —— **不留任何 sql 文件**，同库建同构备份表（执行走个人 skill `mysql-connect`）：

```sql
CREATE TABLE `t_<YYYYMMDD>` LIKE `t`;
INSERT INTO `t_<YYYYMMDD>` SELECT * FROM `t`;
```

备份表承载**当时的旧结构与旧数据**，故回滚天然把结构一起带回旧态。

**收尾（二选一）**
- 测试**通过** → 删除备份表。
- 测试**不通过** → 删原表、给备份表改名回原名：
  ```sql
  DROP TABLE `t`; RENAME TABLE `t_<YYYYMMDD>` TO `t`;
  ```

**执行边界**
- ⚠ **我永不主动删 / 改备份表**。只在**你通知「删除备份」**时才动，且**按备份表名筛选**（查 `information_schema.tables` 命中项）后 `DROP`，**不凭记忆列清单**。判「通过 / 不通过」的是**你** —— 我不跑测试。
- 你可能自主删除备份表 → 我不假设备份表一定存在，发现残留也不算异常。
- 多表改动：逐表各备一份，**同批备份、整批回滚**。
- 命名 `<表名>_<YYYYMMDD>`；**同名备份表已存在时不得覆盖**（加序号或先问）。
- 备份后核对行数一致（`SELECT COUNT(*)` 对照）。
- 本仓库 36 张表**零外键**，故 `LIKE` 不带 FK 不是问题；将来若加外键，回滚改名后需**手工重建**。

## 五、运行时环境与「跑项目」的例外

**环境**：ECS `123.56.117.17` 上部署 `nacos` / `mysql` / `redis` / `gateway` / 3 个前端。

- MySQL `123.56.117.17:3306`，库 `panoramic_mall`。前端（nginx）：**admin `:81` / store `:82` / mall `:83`**。网关在 Nacos 里注册的是**内网** `172.30.116.38:8080`。
- **本地隧道**：`gateway` 与 `nacos` 各一条 → `localhost:8080` 是网关入口。
- **反向隧道**：3 个端 BFF 各一条 —— 这正是它们注册成 `127.0.0.1:8084` / `8085` 的原因。**`discovery.ip: 127.0.0.1` 是有意为之，别当调试残留修。**

**故你明确要求跑项目时**：**基础服务一个都不用起**（nacos / mysql / redis / gateway / 前端都在 ECS），**只启动对应的目标服务**，例 `mvn -pl store spring-boot:run`。
⚠ 这是**例外**，默认仍不跑；起停配方见 memory `local-service-run-stop-recipe`。

## 六、代码生成与分层约定

以下违反**都不报错**，故必须成文。

- **CRUD 交 MyBatis-Plus 基类**：每个实体一个独立 service，接口 `extends IService<T>`、实现 `extends ServiceImpl<XxxMapper, Xxx>`；own-entity 的增删改查直接用基类方法，不新写逻辑、**不直接用 own Mapper**。**纯关联表**（如 `sys_user_role`）也要建 service。
- **审计字段交自动填充，禁止手写赋值**：业务实体一律 `extends BaseEntity`，`create_user` / `update_user` / `create_time` / `update_time` 由 `common` 的 `MyMetaObjectHandler` 经 `UserContext` 填充 —— 代码**不得**出现 `setCreateUser` / `setUpdateUser` / `setCreateTime` / `setUpdateTime`，也不得绕过 `save` / `updateById` 等触发填充的基类方法。（纯关联表无审计列、物理删除，除外。）
- **跨实体只走 owner service**：service 内**禁止**直接持有 / 调用其他实体的 Mapper；要读写别的实体，必须调该实体自己的 service（对方缺能力时先在对方 service 上加方法再回来调）。
- **内容归其所属实体的 controller**：要查什么内容，就调什么实体的 controller / service。例：不要在 `RoleController` / `RoleService` 里查 User 相关内容。
- **循环引用**用 Spring `@Lazy` 断环，需在模块 `lombok.config` 加 `lombok.copyableAnnotations += org.springframework.context.annotation.Lazy` 使其进入构造参数。

## 七、前端三端统一形态

`frontend/` 下 `admin` / `store` / `mall` 技术形态一致（Vue 3 + Vite + TypeScript + axios），各自 `package.json` / `node_modules` 独立。**通用写法见 [`frontend/README.md`](frontend/README.md)**，以下是几处**不得单边改动**的硬约束：

- **`tsconfig.json` 三份内容一致**，唯一差异：**admin / store 多一项 `element-plus/global`**（这两端全局注册 EP，模板里 `<el-*>` 才拿得到类型）。⚠ **不要**为「和 mall 对齐」删掉它 —— 删了模板里所有 EP 组件全报错。**改 tsconfig 选项时三端同改。**
- **不许为了让错误消失而放松门禁**：不关 `strict`、不把 `noUnusedLocals` / `noUnusedParameters` 打开变关闭、不 `exclude` 掉报错文件、不写 `any` / `as any` / `@ts-ignore` / `@ts-nocheck`。
- **类型放哪**：只在**跨文件复用**时放 `src/types/` 与各 `src/api/*.ts`（`RespData<T>` / `PageResult<T>` / 分页参数 / 登录用户等）；**页面私有的表单对象、列表行**就近在各自 SFC 内声明，不外移。字段照后端 VO/DTO 写，**可空列一律 `| null`**。
- **`frontend/admin/src/api/request.ts` 与 `frontend/store/src/api/request.ts` 必须逐字相同**（仅头部互相指认端名的两条注释不同）。这是刻意的重复（各自独立工程），**改一处要同步改另一处**。
- 写 / 改前端**只照 `docs/contracts/<端>.md` 写，不照后端代码写**。

## 八、元规则

- **歧义先问**：规则适用或归属有歧义时，**先向你确认**，不自己判断。
- **文档只分两类**：**项目描述**（各 README 的架构位置 / 实体标记 / 职责与边界 / 配置说明 / 约定与注意事项 —— ⚠ **「注意事项」也算项目描述，不是规约**）与**契约登记**（`docs/contracts/**`）。
  - 清规约时**只动治理性文件**，模块 README 的边界节一律不碰 —— 它们被契约表 **9 处**引用为「规则本体」，删了契约当场断链。
  - 描述文档与现状漂移时，处置是**订正内容**，**不是删除**（删即把「过期描述」误判成「多余规约」）。
  - **不要往回收进契约表** —— 那违反本仓库自己的「契约文件只写接口与形状，❌ 不写业务规则散文」。
