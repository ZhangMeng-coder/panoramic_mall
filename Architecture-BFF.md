# 全景商城 · 实体 / 业务域 / 服务 分析（BFF 架构草案 v1）

> 目的：把「现有实体 + 规划实体」按业务域分组，落到 BFF（端）+ 下沉域（业务微服务）两层结构上，供逐条拍板。
> 标注约定：**现状**=代码/表已落地；**规划**=Architecture.md 提到待做；**推导**=当前清单没明列但业务流必需；**⚠**=归属我拿不准，列在文末待拍板。

---

## 1. 实体清单（中文名）

| # | 实体（中文名） | 来源 | 一句话说明 |
|---|---|---|---|
| 1 | 平台管理员用户 | 现状 | 平台后台账号（sys_user） |
| 2 | 平台角色 | 现状 | 平台后台角色 |
| 3 | 平台权限 | 现状 | 菜单 / 按钮级权限点 |
| 4 | 用户-角色关联 | 现状 | 纯关联表 |
| 5 | 角色-权限关联 | 现状 | 纯关联表 |
| 6 | 商品分类 | 现状 | 多级树，如手机→品牌 |
| 7 | 商品品牌 | 现状 | 品牌字典 |
| 8 | 标准商品 SPU | 现状 | 中台信息模板（无店、无价格库存，注明"无上下架"） |
| 9 | 标准商品 SKU | 现状 | 模板的规格组合（无价格库存） |
| 10 | 店主账号 | 现状 | 商户独立账号体系（store_user） |
| 11 | 店铺 | 现状 | 入驻资质 + 审核状态机（草稿/待审/通过/驳回） |
| 12 | 店铺在售 SPU | 规划 | 店主实际卖的卖品（可来自模板快照或自建），属"谁的东西谁管理" |
| 13 | 店铺在售 SKU | 规划 | 在售 SKU，**带价格/上下架**，可引用模板 SKU 或自建 |
| 14 | 店铺库存 | 规划 | 在售 SKU 维度库存 ⚠（可并入 #13 或独立） |
| 15 | 购物车 | 规划 | 顾客暂存清单 ⚠ |
| 16 | 订单 | 规划 | 主单 |
| 17 | 订单明细 / 商品项 | 规划 | 下单时对在售 SKU 的**快照**（价格/信息/店铺） |
| 18 | 支付记录 | 规划 | 假支付流水（业务流：待支付→已支付） |
| 19 | 物流 / 发货单 | 规划 | 店铺填物流号→已发货 |
| 20 | 退款 / 退货单 | 规划 | 退款退货流程（平台可介入） |
| 21 | 评价 | 规划 | 顾客对订单的评价，分数影响店铺信誉 |
| 22 | 店铺信誉评分 | 规划 | store 域明确职责（未做） |
| 23 | 广告位 | 规划 | Architecture.md 单列，归属 ⚠ |
| 24 | 顾客账号 | 推导 | 业务流 6 明确"用户需登录下单"，后端至今没有顾客主体，必须补 ⚠ |
| — | （收货地址） | 推导/可选 | 下单收货必需，本轮不主列，建议补 |
| — | （商品合并申请） | 推导/可选 | 业务流 2：店主把自认为标准的商品提交中台审核合并（goods-center 内可选实体） |

---

## 2. 按业务域分组

分组原则：**数据归"拥有它的业务域"管（谁的东西谁管理）；一份数据被多个端读/写 → 它就是共享域，以领域接口暴露，BFF 不复制**。

### 域 A · 平台账号与权限域 —— 归属 `admin`
实体：1 平台管理员用户、2 平台角色、3 平台权限、（4、5 关联表）
- 只被平台后台这一个端使用 → 可与 admin 端 BFF **合一**（已达成一致）。
- 平台账号信息仅被店铺审核留痕引用（只记 id，不联查）→ 不必开共享接口。

### 域 B · 商品域（标准商品中台）—— 归属 `goods-center`
实体：6 商品分类、7 商品品牌、8 标准商品 SPU（模板）、9 标准商品 SKU（规格组合）
- 它是**平台侧的标准资产 / 录入加速字典，不是顾客目录**。
- 消费方：店主建品时从中取模板快照（store-bff 调用，供店主选模板）；平台维护/标准化审核（admin 端调用）。
- 顾客浏览读的是"店铺上架在售品"（域 C），**不直连本域**。
- 可选：商品合并申请（店主提交→平台审核）。

### 域 C · 店铺与商铺商品域 —— 归属 `store`（下沉纯域）
实体：11 店铺、22 店铺信誉评分、12 店铺在售 SPU、13 店铺在售 SKU、14 店铺库存
- 数据被多端读/写 → 共享下沉域，统一经 store-api 暴露：店铺资料/审核状态/信誉 → admin（审核/运营）；上架目录 + 信誉 → mall（顾客浏览）；店主自己的店铺/商品 → store-bff（店主后台编排）。
- 【账号店同 ID（2026-09-07 落地）】store_shop 删除 `owner_user_id`（连同其 UNIQUE），改「店铺主键 id == 店主账号 id」（一人一店）；店主范围判定 =「store_id(=账号 id)==店铺主键」，域内按 **store_id 通用数据权限**适配（owner 只作用于「id==store_id 的店」、platform 全量），本域**不持有 store_user 表**即可完成店主身份的作用域校验。

### 店主账号（store_user）—— 端私有，归属 `store-bff`
- store_user 与 store_shop 本就是两张表、职责可拆：store_user 只被店主端登录/签发这一个端使用 → 命中"只被一个端用 → 可随端"的判据，随 store-bff。
- 「账号店同 ID」即唯一关联：store_shop.id == 店主账号 id（一人一店），跨服务只作 id 引用、不做联查；store-bff 登录后以**自己账号 id 作为 store_id** 调 store 域 owner 接口（无需按 owner_user_id 反查"我的店"）。

### 域 D · 消费者与交易域 —— 归属 `trade-center`（未来下沉，当前未建）
实体：24 顾客账号、15 购物车 ⚠、16 订单、17 订单明细（快照）、18 支付记录、19 物流/发货单、20 退款/退货单、21 评价
- 数据被三端消费（mall 下单看单、store 接单发货、admin 运营介入/报表）→ 是典型**共享域，必须下沉独立服务，不与 mall-bff 合一**。
- 订单明细做成下单快照，正好呼应你"模板快照 + 接受过期"的一致性哲学。
- 已知两处跨域写：**下单扣库存 → 写 store 域在售库存**；**评价完成 → 回写 store 域店铺信誉**（详见文末待拍板 4）。

### 广告位 —— ⚠ 未定
实体：23 广告位。候选取向：平台运营维护（admin 侧）+ 商城端读取展示；或独立"展示内容域"。见待拍板 1。

---

## 3. 服务总览

```
前端页面          admin 后台端 / store 店铺端 / mall 商城顾客端
                 ┌──────────────────────┬──────────────────────┬─────────────────────┐
  BFF / 端层      │ admin(合一 sys_*)    │ store-bff(仅持 store_user) │ mall-bff(纯聚合)   │
                 └──────────┬───────────┴──────────┬───────────┴─────────┬───────────┘
                            ▼ 内部调用(注册中心Feign, 不再走网关暴露给页面)
  下沉域服务  goods-center(商品域)  │  store(店铺与商铺商品域,store-api)  │  trade-center(未来,消费者与交易域)
```

| 服务名 | 层 / 形态 | 拥有数据域 | 状态 |
|---|---|---|---|
| admin | 端 BFF + 域 A **合一** | 平台用户/角色/权限 | 现状已有 |
| goods-center | 下沉纯域 B | 分类/品牌/标准 SPU/SKU 模板 | 现状已有 |
| store-bff | 端 BFF（只持 1 个实体 store_user） | 店主账号（登录签发） | 现状已有（从 store-center 拆出） |
| store | 下沉纯域 C，对他人暴露 store-api | 店铺/信誉/在售品/库存 | 现状已有（在售品等待扩展） |
| mall-bff | 端 BFF，纯聚合 | 几乎无（⚠ 购物车端私有可选） | 待建 |
| trade-center | 下沉纯域 D | 顾客/购物车/订单/支付/物流/退款/评价 | 待建 |

网关：职责从"路由到域服务"改为"路由到 BFF（+全局粗校验/放行公开路由）"；域服务不再暴露公网路由。BFF↔域走注册中心内部调用。**落地状态**：
- 2026-09-06（admin↔goods-center 切片）：common 落内部 Feign 客户端（`com.panoramic.common.goods.api`）+ resilience4j 熔断 + 信任头（`X-Internal-Token`），goods-center 收口 `/internal/goods/**` 纯域、admin 做 BFF 编排。
- 2026-09-07（store-center 下沉切片）：store-center 拆为 **store**（下沉纯域，仅持 store_shop，`/internal/store/**`）+ **store-bff**（店铺端 BFF，持 store_user）；账号店同 ID（D4）、store_id 通用数据权限（D5）、admin 不读账号（D6）；common 上移 `com.panoramic.common.store` 共享类型 + `StoreClient`；admin 店铺管理 BFF 编排、网关白名单收敛为 `admin,store-bff`。
- mall/trade 等其余切面待后续按同规约补齐。

---

### admin —— 平台运营端 BFF + 平台账号与权限域（合一）

- **数据域**：平台管理员用户 / 角色 / 权限（sys_*）。
- **大致功能**
  - 平台账号、角色、权限（菜单/按钮）管理，登录签发（平台身份）。
  - 平台运营：审核店铺、停用店铺/停上架商品、维护商品中台模板、平台总体报表（聚合多域）。
- **提供的接口（面向自身前端）**
  - 【现状已有】`auth/login`、`auth/logout`、`auth/me`、`auth/password`；
  - 【现状已有】`users`（分页/CRUD/绑角色/未分配用户）、`roles`（CRUD/分配权限/分配用户）、`permissions`（tree/menus/CRUD）。
  - 【规划】店铺审核/停用、商品停用、平台报表（这些作为编排，数据从下述域取）。
- **它调用（消费）**：goods-center（模板维护、合并审核处理）、store（店铺查询/审核/信誉/上架品，经 store-api）、trade-center（会员/订单/报表，未来）。
- **备注**：判据命中"数据只被一个端用"→ 端+域合一成立。

### goods-center —— 商品域（标准商品中台，下沉纯域）

- **数据域**：分类 / 品牌 / 标准 SPU / 标准 SKU 模板（+可选 合并申请）。
- **大致功能**
  - 平台标准化维护标准商品库；向店主建品提供"简化输入"（条码/关键字检索→快照）。
  - 接收店主提交的"标准商品合并申请"并做平台侧审核合并。
- **提供的接口（共享领域面，经内部调用被各 BFF 消费）**
  - 【已收敛为内部接口（2026-09-06）】基础路径 `/internal/goods`：`brands`（CRUD/page/list）、`categories`（CRUD/tree）、`spu`（CRUD、`/skus`、`/status`）；返回业务原类型不包 RespData，入口按 `goods:*` 权限码域内判定，经 Feign 被 admin 消费（`GoodsCenterClient`）。
  - 【规划/面向 store】模板检索、模板详情快照、分类/品牌下拉（店主建品用，store-bff 就绪后走同 Feign 通道）。
  - 合并申请审核：按已拍板不做。
- **谁调它**：store-bff（建品导模板/同步覆盖）、admin（中台维护/审核）。mall 不直连本域。
- **备注**：纯域下沉，BFF 层不落本域的表；它不需要向别人暴露"页面形状"的接口。

### store —— 店铺与商铺商品域（下沉纯域）

- **数据域**：店铺 store_shop、店铺信誉评分、店铺在售 SPU、在售 SKU、（库存 ⚠）。
- **大致功能**
  - 以领域能力暴露"店铺 + 在售商品"整条数据，供三类主体消费：admin（平台身份）店铺审核/查询/停用、上架商品运营；mall（公开）店铺资料/信誉/上架目录；store-bff（店主 owner）自己店铺与在售商品的操作。
  - 在售商品建模（从中台模板导入生成 / 自建、填价格库存、上下架、模板同步覆盖）作为本域领域动作。
- **提供的接口（共享面 store-api，Feign，已收敛为内部 `/internal/store/**`，2026-09-07）**
  - 店铺：owner=「mine/save/submit」（store-bff 调用，必带 store_id）、platform=「page/detail/audit」（admin 调用，不传 store_id 全量）；停用、资料更新、信誉详情等后续按同规约补齐。
  - 在售商品（后续切片）：按店 CRUD、由模板生成在售、模板同步覆盖、上下架、上架目录查询（公开读）。
  - 原 store-center 的 AdminShopController（平台身份审店）→ 本域 platform 能力；原 ShopController（mine/save/submit）→ store-bff 编排后落本域 owner 操作。
- **谁调它**：admin、mall-bff、store-bff 三个 BFF 都消费；trade-center（未来）跨域写（扣库存/回写信誉）。
- **备注**：纯共享域，不带店主登录与页面编排；本域按调用意图（X-User-Type）与数据作用域约束：owner=「id==store_id 的店」（账号店同 ID，防越权）、platform(admin)=全量、mall=公开读上架；owner 归属收敛在 store-bff，域内只做执行 + store_id 校验（D5）。不持有 store_user，admin 不读店主账号（D6）。

### store-bff —— 店主端 BFF（只持 1 个实体：store_user）

- **数据域**：店主账号 store_user（登录签发用；店主无角色权限树，鉴权 = 登录后对自己店全权限）。
- **大致功能**
  - 店主注册/登录/签发、店主会话（store_user 是 store-bff 唯一自有实体）。
  - 店主后台页面编排：入驻与店铺资料（→store）、我的商品（模板检索→goods-center、在售 CRUD/同步→store）、我的订单履约（→trade-center，未来）、经营报表（聚合）。
  - 账号店同 ID：登录后**账号 id 即 store_id**，mine/save/submit 直接以账号 id 作 store_id 调 store 域 owner 接口（无需反查"我的店"）。
- **提供的接口（店主端，给 store 前端，路径不变 2026-09-07）**
  - 【现状已有】`/store/auth`(register/login/logout/me)、`/store/shops`(mine/save/submit——经 StoreClient 编排到 store 域 owner 接口)。
  - 【规划】我的商品管理、订单履约、报表。
- **谁调它**：只有 store 前端。
- **备注**：借 store_user 与 store_shop 两张表的天然切缝，把"端私有登录"和"共享店铺数据"拆开，比合一两脸更干净——共享数据域统一下沉、端私有统一随 BFF，只有 admin(sys_*) 因纯端私有而合一。落地为「账号店同 ID」后无额外反查开销：store_id=账号 id。原 store-center 拆解：store_user→store-bff；store_shop 及将来在售商品→store。

### mall-bff —— 商城顾客端 BFF（纯聚合，待建）

- **数据域**：几乎无；可选购物车作为端私有状态 ⚠（见待拍板 2）。
- **大致功能**
  - 顾客浏览：首页/商品列表/商品详情/店铺主页 = **聚合** store 域的 上架目录 + 店铺信誉（经 store-api，+广告 ⚠）。这是 BFF 最典型的价值点，避免顾客端一次拉多个域。
  - 顾客登录/注册（主体在 trade-center）、购物车、下单、支付（假）、我的订单、评价（编排 trade-center）。
- **提供的接口（面向自身前端，全部规划）**：公开浏览接口、会员接口、交易编排接口。
- **谁调它**：只有 mall 前端。
- **备注**：不拥有数据（除端私有）；是公开高 QPS 读的入口，将来要独立伸缩/缓存/只读副本，最需要与后台管理流量解耦的一层。

### trade-center —— 消费者与交易域（下沉纯域，待建）

- **数据域**：顾客账号、购物车 ⚠、订单、订单明细（快照）、支付记录、物流/发货单、退款/退货单、评价。
- **大致功能**
  - 顾客账号/登录签发、收货管理（如补收货地址）。
  - 购物车；下单（校验在售、锁定**下单快照**）；支付状态机（假）；发货/收货状态机；退款退货（平台介入）。
  - 评价收集 → 计算并回写店铺信誉（跨域写 store-api）。
- **提供的接口（共享领域面，Feign）**
  - 会员认证（mall 登录用；store 履约/退款、admin 会员运营要买家信息 → 查询）。
  - 订单查询与状态流转（store 接单发货、admin 介入/运营报表、mall 我的订单）。
  - 购物车、下单、支付、评价提交。
- **谁调它**：mall-bff（浏览所需登录/购物车/下单）、store（订单履约）、admin（运营/报表/介入）。
- **备注**：数据被三端消费 → 纯下沉域，不与 mall-bff 合一；下单扣库存、评价写信誉是已知跨域写，要在文档/代码里明确通道与一致性口径（现阶段规模可同步，留最终一致伏笔）。

---

## 4. 跨域调用一览（谁调谁、为什么）

| 调用方 | 调用目标 | 目的 |
|---|---|---|
| admin | goods-center | 标准模板维护、店主合并申请审核 |
| admin | store | 店铺查询/审核/停用、上架商品运营（平台身份，经 store-api） |
| admin | trade-center（未来） | 会员/订单运营、平台报表、退款介入 |
| store-bff | goods-center | 店主建品导模板/检索、一键同步覆盖 |
| store-bff | store | 店主自己的店铺资料、在售商品操作（owner 范围，经 store-api） |
| store-bff | trade-center（未来） | 我的订单履约：接单/发货/退款 |
| mall-bff | store | 上架目录、店铺资料/信誉（公开读，经 store-api） |
| mall-bff | trade-center（未来） | 顾客登录/购物车/下单/支付/评价 |
| trade-center（未来） | store | 下单扣库存、评价回写信誉（跨域写，经 store-api，⚠） |
| admin | goods-center | ✅ 已落地（2026-09-06）：Feign + 熔断 + 信任头，见 common `GoodsCenterClient` |
| admin | store | ✅ 已落地（2026-09-07）：平台店铺分页/详情/审核，见 common `StoreClient` + admin `StoreShopBffService`（不读店主账号） |
| store-bff | store | ✅ 已落地（2026-09-07）：店主 mine/save/submit，store_id=账号 id，见 common `StoreClient` + store-bff `StoreShopBffService` |
| store-bff | goods-center | 待后续切片（店主建品导模板/同步覆盖，common Feign 基建已就绪） |

---

## 5. 待你拍板的点（⚠）

1. **广告位归属**：平台运营维护（admin 侧，暴露给 mall 读）？还是独立"商城展示/内容域"？
这个加入内容域，其实三端都在使用，store申请，admin审核，mall预览
2. **购物车**：进 trade-center（随顾客数据）还是 mall-bff 端私有状态？
这个加入mall-bff，购物车只是临时状态的持久化数据，肯定先转换成订单再传给订单服务
3. **库存**：并入在售 SKU 字段，还是独立库存记录（便于扣减防超卖）？我倾向"随在售 SKU + 乐观锁扣减"，少一张表。
库存单独记录
4. **评价 → 信誉 的跨域写**：交易域算分后写 store-api；现阶段同步可接受，但要在文档写明（将来可能异步/最终一致）。
这个只要求最终一致性，并且商品查询时排序需要该字段作为排序条件之一
5. **收货地址**：本轮是否补（下单必用，归属 trade-center）？
这里先不做
6. **商品合并申请**：店主提交→平台审核这条流，是否在 goods-center 内实体化（建审核流），还是先不做合并仅做"店主导入/自建"？
这个先不做